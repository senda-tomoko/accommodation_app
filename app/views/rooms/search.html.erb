<h1>検索結果</h1>

<% if @rooms.any? %>
  <p><%= @rooms.count %>件の施設が見つかりました。</p>

  <div class="rooms-list">
    <% @rooms.each do |room| %>
      <div class="room">
        <h2><%= room.name %></h2>
        <% if room.image.attached? %>
          <%= image_tag room.image %>
        <% else %>
          <%= image_tag 'hotel.png' %>
        <% end %>
        <p><%= room.address %></p>
        <p><%= room.detail %></p>
        <p>宿泊料金: <%= number_to_currency(room.price, unit: '￥', precision: 0, format: "%u%n円~") %></p>
        <%= link_to '詳細はこちら', new_room_reservation_path(room_id: room.id), class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

<% else %>
  <p>該当する施設が見つかりませんでした。</p>
<% end %>
